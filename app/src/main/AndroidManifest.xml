<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cse110">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".View.history.HistoryCategoryActivity"></activity>
        <activity android:name=".View.history.HistoryDetailedActivity" />
        <activity android:name=".View.history.HistoryActivity" />
        <activity android:name=".View.SettingsActivity" />
        <activity android:name=".View.ExpensesListActivity" />
        <activity android:name=".View.CreateAccountActivity" />
        <activity android:name=".View.AccountRecoveryActivity" />
        <activity android:name=".View.CategoriesListActivity" />
        <activity android:name=".View.graphs.GraphsActivity" />
        <activity android:name=".View.MainActivity" />
        <activity android:name=".View.MeetTheTeamActivity" />



        <activity
            android:name=".View.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>